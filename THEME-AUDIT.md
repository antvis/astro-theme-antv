# dumi-theme-antv 视觉审计与迁移

审计来源：`/Users/liufu/publicWorkspace/dumi-theme-antv`，并结合 G2 原有 `site/.dumirc.ts` 中的品牌素材配置。

## 设计读取

这是面向开发者的高密度文档主题。整体以白色和冷灰为底，使用 AntV 紫色表达品牌和交互状态，首页比文档页更具品牌展示感。

设计参数：

- `DESIGN_VARIANCE: 4`
- `MOTION_INTENSITY: 3`
- `VISUAL_DENSITY: 7`

## 原主题关键变量

| 类别     | 原主题取值                   | 新主题映射                     |
| -------- | ---------------------------- | ------------------------------ |
| 主色     | `#873bf4`                    | `--brand`                      |
| 强主色   | `#691eff`                    | `--brand-strong`               |
| 浅紫背景 | `#f9f0ff`                    | `--brand-soft`                 |
| 首页渐变 | `#ffffff` 到 `#f0f5ff`       | `--surface` 到 `--brand-faint` |
| 正文     | `#424e66`、`rgba(0,0,0,.85)` | `--text`、`--text-strong`      |
| 次级文字 | `#697b8c`、`#a3b1bf`         | `--muted`、`--muted-light`     |
| 分隔线   | `#f0f0f0`、`#e5e8ef`         | `--border-soft`、`--border`    |
| 圆角     | 6、8、16px                   | small、medium、large 三档      |
| 导航高度 | 64px                         | `--header-height`              |
| 内容宽度 | 1440px                       | `--content-width`              |
| 左侧目录 | 280px                        | `--docs-sidebar-width`         |
| 右侧目录 | 260px                        | `--toc-width`                  |

## 保留的识别性模式

- 64px 白色顶部导航，AntV 标识、分隔线和项目名称组合展示。
- 导航当前项使用顶部紫色细线，文字 hover 使用品牌紫。
- 首页使用白到淡蓝紫渐变、G2 原有主视觉和 16px 圆角内容卡片。
- 桌面端内容容器取消固定最大宽度，随视口全宽响应，并保留页面安全边距。
- 文档页使用左侧分级目录、中间正文、右侧页内目录的三栏结构。
- 文档目录与示例目录共享 `SidebarMenu` 组件，统一使用 14px 菜单字号、40px 行高、右侧展开箭头、浅紫选中背景和 3px 紫色右侧选中线。
- 文档目录沿用原主题 `inlineIndent={16}` 的紧凑层级；示例目录保留 16px 半透明分类图标和 48px 二级缩进。分类图标直接使用旧主题 iconfont 的原始字形轮廓，不依赖运行时外链脚本。
- 正文标题采用 30、24、20、16px 的紧凑层级，正文保持高行距。
- 示例画廊使用 4 列缩略图、20px 分组标题和 16px 居中图表标题，hover 时上移并显示浅色投影。
- 浅色代码块和细分隔线继续作为默认阅读风格。

## 有意调整的部分

- 保留静态 HTML 路由、Pagefind、严格 sandbox 和初始 HTML 源码，不迁回 dumi 的客户端路由与异步内容模型。
- 原主题依赖 Ant Design、React、Monaco 和大量运行时状态。新主题使用 Astro 与原生 HTML，视觉相近但依赖更轻。
- 原主题文档目录由 dumi 菜单数据驱动。新主题根据文档路径构建分级目录，并允许项目通过 `content.sidebar` 提供双语分组名称和目录级 `order`；文档叶子继续使用 frontmatter `order`，每层独立排序。
- 文档与示例保留各自的数据适配逻辑，最终都转换为共享的静态菜单节点。共享层只负责层级、图标、当前页、展开状态和响应式样式，不耦合项目路由规则。
- 原主题主要为浅色模式。新主题增加同一紫色品牌体系下的暗色变量，不改变页面信息结构。
- 原主题 demo 页的分栏编辑器不适合 AI 抓取和严格 sandbox。新主题保留纵向的运行结果与完整源码结构。

## 可配置品牌素材

共享主题不硬编码 G2 素材，消费者可配置：

- `site.logo`：顶部 AntV 或项目标识。
- `home.image`：首页主视觉。
- `home.features[].icon`：特性图标。
- `content.sidebar`：文档目录路径对应的双语名称和可选排序值。
- `examples[].icon`：示例一级分类图标；可使用旧主题的命名字形或图片 URL。主题已本地化 G2、G6、X6 常用的 `gallery`、`data`、`net`、`map`、`interaction`、`shape`、`tree`、`line`、`area`、`bar`、`pie` 等字形，未知名称稳定回退到 `other`。

G6、X6 接入时只需替换这些素材和内容配置，布局、色彩、响应式规则及静态能力可直接复用。

## 验收限制

实现环境没有可连接的浏览器实例，因此当前完成的是源码、构建产物、响应式规则和语义结构验证。最终仍需在真实浏览器中对首页、文档页、示例列表、demo 页的桌面与移动端效果进行视觉验收。
